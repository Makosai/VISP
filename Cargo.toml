# Generate Tailwind
# npx tailwindcss --config ./tailwind.config.js -i ./input.css -o ./public/assets/css/tailwind.css --minify --watch

# Dev
# dx serve --hot-reload --platform desktop --features=desktop

# dx serve --hot-reload --platform web --features-web

# Build Instructions
# dx bundle --platform desktop --features="desktop" --release --verbose
# dx build --platform web --features="web" --release

# dx build --bin visp_desktop --release or dx bundle --bin visp_desktop --features bundle --release

# dx build --bin visp_web --release or dx serve --bin visp_web --release

[package]
name = "visp"
version = "0.1.0"
edition = "2021"
authors = ["Kristopher Ali <kristopher.ali.dev+visp@gmail.com>"]
description = "Video. Images. Sound. Post. VISP is an open source multimedia editor built entirely using Rust. It works on all Desktop and Mobile platforms. And you can integrate it with your favorite social media sites like YouTube and Instagram!"
resolver = "2"

[package.metadata.bundle]
name = "VISP"
identifier = "io.kristech.visp"
version = "0.1.0"
copyright = "MIT License Copyright (c) 2023-2024 Quaint Studios. All rights reserved."
category = "Multimedia"
short_description = "Video. Image. Sound. Post. A versatile tool."
long_description = """
Video. Images. Sound. Post. VISP is an open source multimedia editor built entirely using Rust. It works on all Desktop and Mobile platforms. And you can integrate it with your favorite social media sites like YouTube and Instagram!
"""

[dependencies]
dioxus = { version = "0.5.0-alpha.0" }
dioxus-router = { version = "0.5.0-alpha.0" }
manganis = { version = "0.1.1" }

[target.'cfg(windows)'.dependencies]
platform-dirs = "0.3.0"

[features]
default = []
desktop = ["dioxus/desktop"]
web = ["dioxus/web"]